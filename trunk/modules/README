gpsim modules

This directory contains modules dynamically loaded by gpsim. Currently, if you want to create a custom module you'll need to hack the source. Eventually (release 0.21.0?) I'll add true dynamically loaded modules.